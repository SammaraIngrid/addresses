<%= render partial: 'layouts/components/errors', locals: { resource: @address } %>

<%= form_with(model: @address, local: true) do |form| %>
  <%= render partial: 'addresses/components/form', locals: { form: form } %>
<% end %>
